{
  "schema_version": 1,
  "task_id": "configuration-overlay",
  "title": "Repair immutable recursive configuration overlays",
  "prompt": "Repair the candidate implementation so it satisfies its documented API and public tests. Preserve the public function signature, recursive semantics, and input isolation. Mapping keys at every level must be strings; raise ValueError if either input contains a non-string key. You may add focused public regression tests. Do not access external resources. Finish with the required JSON completion.",
  "public_command": ["python", "-m", "unittest", "discover", "-s", "tests", "-v"],
  "hidden_command": ["python", "/evaluator/test_hidden.py"],
  "seed_public_outcome": "succeeded",
  "timeout_seconds": 30
}
